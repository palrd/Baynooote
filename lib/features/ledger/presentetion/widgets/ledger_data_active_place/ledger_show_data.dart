import 'package:flutter/material.dart';

///该组件是当活动区中有数据时展示的组件
///在这个组件中，需要根据"快速记录卡片的状态来决定展示哪种类型的内容"
///包括：
///1.点击"输入记账金额"时，展示一个输入金额的键盘，以及展示输入内容的实时展示
///2.点击"时间类型图标时，展示一个按照时间推荐的记账卡片
///3.点击铅笔时，展示完整的默认记账卡片
class LedgerShowData extends StatelessWidget {
  const LedgerShowData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
